'use client'

import { FunctionComponent } from 'react'

type ProfilePageProps = {

}

const ProfilePage: FunctionComponent<ProfilePageProps> = (props: ProfilePageProps) => {
  return (
    <main>
      Profile Page
    </main>
  )
}

export default ProfilePage
