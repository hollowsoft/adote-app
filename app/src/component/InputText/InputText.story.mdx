import { Meta, Story } from '@storybook/addon-docs'

import { InputText } from '.'

<Meta title="Input Text" component={InputText} />

export const Template = (props) => <InputText {...props} />

<Story
  name="Input Text"
  args={{}}>
  {Template.bind({})}
</Story>
