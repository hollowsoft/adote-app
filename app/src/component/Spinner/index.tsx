import { FunctionComponent } from 'react'

import { Spinner as Sn } from '@chakra-ui/react'

type SpinnerProps = {

}

export const Spinner: FunctionComponent<SpinnerProps> = (props: SpinnerProps) => {
  return (
    <Sn />
  )
}
