import { FunctionComponent } from 'react'

import { Switch as Sw } from '@chakra-ui/react'

type SwitchProps = {

}

export const Switch: FunctionComponent<SwitchProps> = (props: SwitchProps) => {
  return (
    <Sw />
  )
}
