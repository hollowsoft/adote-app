import { FunctionComponent } from 'react'

import { Image as Im } from '@chakra-ui/react'

type ImageProps = {

}

export const Image: FunctionComponent<ImageProps> = (props: ImageProps) => {
  return (
    <Im />
  )
}
