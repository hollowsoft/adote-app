import { Meta, Story } from '@storybook/addon-docs'

import { Input } from '.'

<Meta title="Input Text" component={Input} />

export const Template = (props) => <Input {...props} />

<Story
  name="Input"
  args={{}}>
  {Template.bind({})}
</Story>
