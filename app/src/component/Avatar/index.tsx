import { FunctionComponent } from 'react'

import { Avatar as Av } from '@chakra-ui/react'

type AvatarProps = {

}

export const Avatar: FunctionComponent<AvatarProps> = (props: AvatarProps) => {
  return (
    <Av />
  )
}
